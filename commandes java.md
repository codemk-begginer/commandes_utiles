### Executer un projet spring-boot depuis le terminal

```bash 
     ./mvnw spring-boot:run

```

### pour creer un jar de son projet spring boot

```bash
      mvn clean install
```
   
### pour executer le jar cree

```bash
      java -jar  core-1.0-SNAPSHOT.jar 
   ```
